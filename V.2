8 GB disk size:
	pay attention at the creation of the VM to give a static size of 8 GB

4.2 GB partition:
	at the installation, specify a 4.2 GB partition. and check with lsblk and correct the size later with fdisk

up to date:
	sudo apt update
	sudo apt upgrade
	sudo apt install
